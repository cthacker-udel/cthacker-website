import { UnderConstruction } from "modules/common/modules/UnderConstruction";
import React from "react";

/**
 * Page detailing all the places I've worked at, along with languages, and tasks.
 *
 * @returns The work history component
 */
export const WorkHistory = (): JSX.Element => <UnderConstruction />;
